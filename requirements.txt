# PipeData Railway 배포용 의존성

# 웹 프레임워크
Flask==2.3.3
Flask-CORS==4.0.0

# HTTP 요청
requests==2.31.0

# 데이터베이스
# sqlite3는 Python 표준 라이브러리에 포함되어 있음

# AI 및 임베딩
openai==1.6.1
anthropic==0.7.8
# sentence-transformers==2.2.2  # 메모리 집약적, 필요시 별도 설치

# 웹 스크래핑
# selenium==4.15.0  # 브라우저 드라이버 필요, Railway에서 복잡함
beautifulsoup4==4.12.2
lxml==4.9.3

# 이미지 처리 (OCR)
# pytesseract==0.3.10  # tesseract 바이너리 필요
Pillow==10.0.1

# 데이터 처리
pandas==2.1.3
numpy==1.24.3

# Excel 파일 처리
openpyxl==3.1.2
xlsxwriter==3.1.9

# 시스템 모니터링 (선택사항)
psutil==5.9.8

# 유틸리티
python-dotenv==1.0.0
schedule==1.2.0

# Reddit API
praw==7.7.1

# 로깅
loguru==0.7.2

# 병렬 처리
# concurrent.futures는 Python 표준 라이브러리에 포함되어 있음

# JSON 처리
jsonschema==4.19.2

# 날짜/시간
python-dateutil==2.8.2

# Railway 배포용 WSGI 서버
gunicorn==21.2.0